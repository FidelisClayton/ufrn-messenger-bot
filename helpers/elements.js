export default function({ title, item_url, default_action, image_url, subtitle, buttons }) {
  return {
    title: title,
    item_url: item_url,
    image_url: image_url,
    subtitle: subtitle,
    buttons: buttons
  }
}
